/*
shiftLeft

void copyPgm(
for(uint8_t i = 0; i < 64; i++)
{
  dst[] = src[]
  
}

struct Program
{
  uint8_t data[8];
}

toggle(x, y)
{
  data[y] ^= 0x1 << (x % 8)
}

uint8_t writeBuffer[32]
for(uint8_t i = 0; i < 64; i++)
{
  led = INACTIVE;
  if(in cursor) led = ACTIVE_LOW
  if(in PGM) led = ACTIVE_HIGH
  writeBuffer[i/2] = led & 0xF << ((x + 1) % 2);
}
*/
